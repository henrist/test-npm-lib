export class ExampleService {
  public multiply = (a: number, b: number): number => a * b;
  public plus = (a: number, b: number): number => a + b;
  public minus = (a: number, b: number): number => a - b;
}
